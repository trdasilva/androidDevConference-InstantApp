apply plugin: 'com.android.instantapp'

dependencies {
    implementation project(':welcomePage')
    implementation project(':base')
}

android {
    compileSdkVersion 26
    buildToolsVersion '26.0.1'
    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
    productFlavors {
    }
}